# odin
The many-named one

# Training the tokenizer

The data used to train the tokenizer was names of inventors and assignees collected from public Google records. The raw and harmonized names were collected and deduplicated.
